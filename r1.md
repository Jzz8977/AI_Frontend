# 简历改写网站 — 需求文档

> 给 Claude Code 的实现说明书。包含产品定位、设计规范、交互流程、Prompt 工程、技术实现细节。
> 目标:用户粘贴简历 → AI 按目标岗位的 2026 标准改写 → 输出可直接用的版本。

---

## 1. 产品概述

### 1.1 核心价值
帮求职者把简历改写成 **2026 年招聘市场想看的样子**,专注前端 / 全栈 / AI 应用三个方向。
不是简单润色,而是按目标岗位的最新技术标准重塑技术叙事 — 强化 AI 浓度、量化产出、现代化技术栈。

### 1.2 目标用户
- 1-5 年经验的前端/全栈开发者
- 想转型 AI 应用方向的工程师
- 简历技术栈偏旧、缺少量化数据的人

### 1.3 核心差异点
- **岗位定制化** prompt,不是通用润色
- **2026 技术词典**(强化项 / 弱化项 / 替换项)内置在 prompt 里
- **AI 浓度兜底**:即使原简历没有 AI 经验,也能基于真实项目能力合理延伸出 AI 工程经验
- **IDE 美学** UI,跟同类紫色渐变 SaaS 拉开差异

---

## 2. 产品形态

### 2.1 技术栈
- **框架**:Next.js 15 (App Router) + TypeScript
- **样式**:Tailwind CSS + CSS Variables(主题色)
- **AI**:Anthropic Claude API (claude-sonnet-4 或更高),通过 Vercel AI SDK 调用
- **部署**:Vercel
- **可选**:后期加 Supabase(用户/历史/付费)

### 2.2 页面结构(MVP)
单页应用,4 个阶段切换:
```
mode (选模式) → role (选岗位) → input (粘原文) → loading → result (结果)
```
顶部固定导航显示当前 step。无需路由,用 React state 管理阶段。

---

## 3. 交互流程详细规格

### 3.1 Step 1 — 选模式

**两张大卡片,横向排列(grid-cols-2):**

| 卡片 | 标识 | 标题 | 副标题(英文小字) | 描述 | 预估耗时标签 |
|---|---|---|---|---|---|
| 模式 1 | MODE_01 | 快速重写 | // auto-rewrite | 选岗位 / 粘原文 / AI 一键改写 / 双栏对比 | ~30s |
| 模式 2 | MODE_02 | 精修诊断 | // diagnose & refine | AI 逐条分析问题 / 你决定接受或修改每条建议 | ~90s |

**卡片样式要求:**
- 黑色面板背景 `#111`,卡片之间用 1px 边框分隔
- 左上角:绿色等宽字体的 `MODE_01` 标识
- 右上角:橙色 outline 标签显示预估耗时
- 大标题用 32px 浅字重无衬线
- 底部:绿色 "启动 →" 提示
- hover 时背景从 `#111` 变 `#161616`

**页面顶部大标题:**
```
把简历写成
2026 年招聘官想看的样子.
```
- 第一行:细字重(font-weight 200)
- 第二行:粗字重 + 斜体,末尾的 `.` 用终端绿色
- 字号 72px,字距 -2px

### 3.2 Step 2 — 选岗位

**三张卡片,等宽并排(grid-cols-3),每张卡片至少 360px 高:**

| 代号(超大显示) | 中文名 | 英文名 | 描述 | 技术标签(底部) |
|---|---|---|---|---|
| **FE** | 前端工程师 | Frontend Engineer | 深度交互 / 性能 / 工程化 | React 19, Next.js 15, TS, Tailwind, Motion, WebGPU |
| **FS** | 全栈工程师 | Full-Stack Engineer | 端到端拥有感 / API / 数据库 / 部署 | Next.js RSC, tRPC, Postgres, Drizzle, Edge, Vercel |
| **AI** | AI 应用工程师 | AI Application Engineer | LLM 集成 / Agent / RAG / 流式 UI | Vercel AI SDK, LangGraph, MCP, RAG, Tools, Streaming |

**卡片样式要求:**
- 代号 56px 等宽超粗字体,终端绿色,字距 -2px
- hover 时整张卡上移 2px(transform: translateY(-2px))
- 技术标签:小号等宽字体 10px,大写,outline 风格(1px 边框,透明底)

### 3.3 Step 3 — 粘贴原文

**核心区域 = 一个仿编辑器的 textarea:**

布局:
```
┌─────────────────────────────────┐
│ resume.txt          0 chars     │ ← 顶部栏,等宽字体灰色
├─────────────────────────────────┤
│                                 │
│ [textarea,最小高度 380px]       │
│ 字体:JetBrains Mono 13px        │
│ 行高 1.7                        │
│                                 │
└─────────────────────────────────┘

[ 加载示例数据 ]            EXECUTE → ← 主按钮:终端绿底黑字
```

**关键细节:**
- 顶部状态条显示文件名 `resume.txt` 和实时字符数
- 按钮 disabled 条件:`text.trim().length < 50`,disabled 时变灰禁用
- 主按钮文案 `EXECUTE`,全大写等宽字体,带 → 箭头
- 当前选中的模式和岗位用 Tag 形式展示在标题下方

**示例数据(点"加载示例数据"填入 textarea):**
```
张三 / 5 年前端
工作经历:
- 2021-至今 XX 科技 高级前端
  负责公司主站重构,使用 Vue + Webpack,优化首屏加载
  参与活动页开发,完成营销活动 20+
  协助后端联调接口,处理 jQuery 老代码

- 2019-2021 YY 公司 前端开发
  使用 React 开发管理后台,实现表单/列表/图表
  负责移动端 H5 开发

技能:HTML/CSS/JS/Vue/React/Node.js
```

### 3.4 Loading 阶段 — 终端动画

居中显示一个仿终端的进度区域:

```
● PROCESSING                    ← 脉冲绿点 + 标签

$ analyzing resume structure... ✓
$ detecting 2026 stack opportunities... ✓
$ injecting AI-related signals... ✓
$ rewriting with quantified metrics... ✓
$ generating diff report... ✓
$ waiting for AI response_      ← 光标闪烁
```

**实现细节:**
- 每行间隔 800ms 出现
- 当前行 = 亮色(`#E8E8E8`),已完成行 = 灰色(`#8A8A8A`)
- 已完成行末尾加绿色 ✓
- 全部出现后最后一行显示橙色 "waiting for AI response" + 闪烁光标
- 文案:**快速重写模式** 用上面那组;**精修诊断模式** 换成:
  ```
  $ scanning resume for issues...
  $ matching against 2026 role spec...
  $ identifying upgrade opportunities...
  $ scoring each section...
  $ compiling diagnostic report...
  ```

### 3.5 Step 4a — 结果页(快速重写模式)

**顶部:**
- 标题:`改写完成.` (句号绿色)
- 右上角 "[ 重新开始 ]" 按钮

**三个 Tab 切换(底部 2px 绿色高亮线):**
1. **双栏对比** (默认)
2. **改写全文**
3. **改动详情**

**通用块:AI Summary 卡片**(三个 Tab 都在顶部显示):
- 小标 `AI SUMMARY` 绿色等宽字体
- 17px 浅字重的总结文字

#### Tab 1:双栏对比
- grid-cols-2,左右各占一半,中间 1px 分隔线
- 左栏:灰色点 + `BEFORE / original` 标签,内容是原文(灰色等宽)
- 右栏:绿色点 + `AFTER / rewritten` 标签,内容是改写版(亮色等宽)
- 两栏内容用 `<pre>` 保留换行

#### Tab 2:改写全文
按顺序展示:
1. **KEY_HIGHLIGHTS**:编号(01/02/03) + 高光要点,每条一行
2. **技能矩阵**:三栏 grid 展示
    - CORE_SKILLS(绿色标题)
    - AI ENGINEERING(橙色标题)
    - EXTRA(蓝色标题)
    - 每个技能用 Tag 组件展示
3. **REWRITTEN_EXPERIENCE**:完整经历(markdown 风格的纯文本)

#### Tab 3:改动详情
每条改动一个卡片:
```
#01  [upgrade]                          ← 编号 + 标签
─────────────────────────────────────
┌─────────────┐    ┌─────────────┐
│ − 原表述    │ →  │ + 改写后    │   ← 红绿对比,中间箭头
│ (红色块)    │    │ (绿色块)    │
└─────────────┘    └─────────────┘
// 为什么 这样改可以...                 ← 橙色 // 注释风格的说明
```

**红色块样式:**
- 背景 `rgba(248,113,113,0.06)`,边框 `rgba(248,113,113,0.2)`
- 文字 `#FCA5A5`,等宽字体
- 内容前缀 `−`

**绿色块样式:**
- 背景 `rgba(63,185,80,0.06)`,边框 `#1F4D26`
- 文字 `#86EFAC`,等宽字体
- 内容前缀 `+`

### 3.6 Step 4b — 结果页(精修诊断模式)

**评分卡(顶部):**
- 左侧 180px 宽:大号分数(56px 粗体),`SCORE` 小标,`/ 100` 灰色
- 右侧:`AI VERDICT` 标签 + 一句话总评
- 分数颜色规则:≥70 绿色,50-69 橙色,<50 红色

**统计行:**
```
FOUND 8 ISSUES                     ACCEPTED 3 / 8
```
左右两端,等宽小字体

**问题卡片列表:**
每条 issue 一个卡片,内部结构:

```
#01  [HIGH]  [缺数据]                      [ACCEPT] ← 右上角按钮
─────────────────────────────────────────────────
原文片段
┌────────────────────────────────────────┐
│ 负责公司主站重构,优化首屏加载          │
└────────────────────────────────────────┘

问题  没有量化指标,看不出实际效果
建议  补充具体性能数据和影响范围

建议改写为 ↓
┌────────────────────────────────────────┐
│ + 主导公司主站架构重构(日 UV 50万),  │
│   首屏 LCP 从 4.2s 优化至 1.1s        │
└────────────────────────────────────────┘
```

**严重度颜色:**
- high → 红色 `#F87171`
- medium → 橙色 `#F0A04B`
- low → 蓝色 `#7DD3FC`

**ACCEPT 按钮交互:**
- 默认状态:透明底 + 绿色边框 + 绿色文字 "ACCEPT"
- 已接受:绿色底 + 黑字 "✓ ACCEPTED"
- 整张卡片在已接受时 opacity 降到 0.6

**底部加上批量操作(可选):**
- "全部接受" 按钮
- "导出已接受的改动" 按钮

---

## 4. 设计规范

### 4.1 色板(IDE / 终端美学)

```css
--bg:          #0A0A0A;  /* 主背景,接近纯黑但略温 */
--panel:       #111111;  /* 卡片背景 */
--panel-2:     #161616;  /* hover/嵌套面板 */
--border:      #222222;  /* 标准边框 */
--border-hi:   #2E2E2E;  /* 高亮边框 */

--text:        #E8E8E8;  /* 主文字 */
--text-dim:    #8A8A8A;  /* 次要文字 */
--text-muted:  #5A5A5A;  /* 极弱文字 */

--green:       #3FB950;  /* 终端绿 — 主强调色 */
--green-dim:   #1F4D26;  /* 绿色暗调,用于边框 */
--amber:       #F0A04B;  /* 警示橙 — 注释/警告 */
--red:         #F87171;  /* 错误红 — 删除/严重 */
--blue:        #7DD3FC;  /* 信息蓝 — 标签 */
```

**核心配色原则:**
- 90% 黑灰底色,只在关键位置打绿色
- 绝对禁用:紫色渐变、彩虹渐变、紫粉色系
- 强调色克制使用,不堆叠

### 4.2 字体

```css
--font-mono: "JetBrains Mono", "SF Mono", "Roboto Mono", Menlo, monospace;
--font-sans: "Inter Tight", "Söhne", -apple-system, BlinkMacSystemFont, sans-serif;
```

**使用场景:**
- **等宽字体(JetBrains Mono)**:所有标签、代号、文件名、终端文本、技能标签、按钮文字、改写内容展示
- **无衬线(Inter Tight)**:大标题、段落正文、AI 总结文字、问题描述

**字号规范:**
- 巨标题(landing 大字):72px,字重 200,字距 -2px
- 二级标题:36-44px,字重 300,字距 -1px
- 卡片标题:22-32px,字重 300-500
- 正文:14-17px,行高 1.6-1.7
- 标签/代号:10-12px,大写,字距 1-2px

### 4.3 间距 & 布局

- 容器最大宽度:1100-1400px(根据页面)
- 卡片之间的分隔不用 gap,而用 **1px 边框** + 父元素 `background: --border` 制造像素分隔线
- 卡片内边距:24-40px
- 顶部导航 padding:14px 32px

### 4.4 复用组件清单

需要抽出来的组件:
- `<Dot />`:小圆点,带 box-shadow 光晕,可选 pulse 动画
- `<Tag />`:outline 风格小标签,可配色
- `<TopBar />`:顶部导航,显示当前 step
- `<LoadingTerminal />`:终端 loading 动画

### 4.5 动效

```css
@keyframes pulse {
  0%, 100% { opacity: 1; transform: scale(1); }
  50%      { opacity: 0.5; transform: scale(0.85); }
}
@keyframes blink {
  0%, 100% { opacity: 1; }
  50%      { opacity: 0; }
}
@keyframes fadeIn {
  from { opacity: 0; transform: translateY(4px); }
  to   { opacity: 1; transform: translateY(0); }
}
```

**应用位置:**
- pulse:顶部 `resume.rewrite()` logo 旁的绿点、loading 阶段
- blink:loading 末尾的 `_` 光标
- fadeIn:loading 终端每一行的出现
- transform:translateY(-2px):岗位卡片 hover

### 4.6 其他细节
- `::selection { background: var(--green); color: #000; }`
- 滚动条用 webkit-scrollbar 改成黑灰色细条
- button focus 去掉默认 outline(自己实现 focus 样式)

---

## 5. Prompt 工程(核心)

> 这是整个产品的护城河,Prompt 决定改写质量。

### 5.1 通用框架

两套 Prompt:
- `buildRewritePrompt(role, original)`:快速重写模式
- `buildReviewPrompt(role, original)`:精修诊断模式

每套都按目标岗位注入不同的 **2026 技术词典**(roleSpec)。

### 5.2 角色规格(roleSpec)

#### 前端 (frontend)
```
- 强化:React 19 / Next.js 15 (App Router/RSC/Server Actions) / TypeScript 严格模式
- 强化:性能 (Core Web Vitals/INP) / 工程化 (Turborepo/Vite 6) / Edge Runtime
- 强化:AI 增强体验 (Vercel AI SDK / 流式 UI / Generative UI)
- 弱化:jQuery / Vue 2 / Webpack 配置细节 / 旧脚手架
- 弱化:CSS 兼容性细节 (除非是大厂方向)
```

#### 全栈 (fullstack)
```
- 强化:端到端拥有感 (前端 + API + DB + 部署)
- 强化:Next.js 15 / tRPC / Drizzle ORM / Postgres / Redis
- 强化:Edge/Serverless 部署 / 可观测性 / CI/CD
- 强化:把纯前端项目往全栈靠 — 突出 API 设计、数据建模、性能优化
- 弱化:框架配置细节 / 单点技术深度
```

#### AI 应用 (ai) — 最关键
```
- 强化:LLM 集成 (OpenAI/Anthropic/开源模型) / 流式响应 / 函数调用
- 强化:Agent 工作流 (LangGraph/CrewAI) / Tools / MCP (Model Context Protocol)
- 强化:RAG (向量库/混合检索/重排) / Embedding / 知识库工程
- 强化:Vercel AI SDK / Generative UI / 流式渲染 / Stream Composition
- 强化:Evals / Prompt 工程 / 成本与延迟优化
- 把任何 AI 相关经历(哪怕只是用过 Cursor/Claude)都包装成工程化经验
```

### 5.3 改写硬性要求(写进 Prompt 里)

```
1. 量化一切:每条经验必须有数字(用户数/性能提升 %/成本下降/耗时减少)。
   原文没有数字就根据项目体量合理推测,但不要夸张。
2. STAR 结构:每条要点遵循 Situation→Task→Action→Result,但写得自然不模板化。
3. 技术栈现代化:用 2026 主流技术名词替换陈旧表述。
   例如 "jQuery 实现交互" → 升级为对应现代方案。
4. AI 浓度:如果原文有任何 AI/LLM/Copilot/Cursor/Claude 相关经历,放大;
   如果完全没有,在"技能"或"项目"中加入合理可信的 AI 工程经验(基于原项目能力延伸)。
5. 去除信息:删掉过时技术(jQuery/Vue 2 配置细节/Webpack 1-3)或弱化为"另熟悉"。
6. 动词强度:用"主导/设计/重构/落地"取代"负责/参与/协助"。
7. 保留真实性:不能编造完全不存在的公司/学历/年限。技能可以基于真实经验合理延伸。
```

### 5.4 输出格式(严格 JSON)

#### 快速重写模式输出
```typescript
{
  summary: string;              // 3 行精炼自我评价
  highlights: string[];         // 3-5 条高光要点,每条带具体数字
  skills: {
    core: string[];             // 核心技能 6-8 项
    ai: string[];               // AI 相关 4-6 项
    extra: string[];            // 加分项 4-6 项
  };
  rewritten_experience: string; // 改写后的完整工作经历(markdown,## 分块)
  diff_notes: Array<{
    type: "upgrade" | "add" | "remove" | "rephrase";
    from: string;               // 原表述
    to: string;                 // 改写后
    why: string;                // 为什么这样改
  }>;                           // 5-8 条关键改动
}
```

#### 精修诊断模式输出
```typescript
{
  score: number;                // 整体评分 0-100
  verdict: string;              // 一句话总评
  issues: Array<{
    id: string;                 // i1, i2, ...
    severity: "high" | "medium" | "low";
    category: "缺数据" | "技术陈旧" | "岗位不匹配" | "表达冗余" | "缺AI浓度" | "结构问题";
    original: string;           // 原文片段(不超过 50 字)
    problem: string;            // 问题是什么
    suggestion: string;         // 建议怎么改
    rewritten: string;          // 直接给出改写后的版本
  }>;                           // 6-10 条
}
```

### 5.5 Prompt 模板末尾

```
直接返回 JSON,不要任何前后缀文字,不要 markdown 代码块包裹。

## 原简历
{original}
```

### 5.6 API 调用与解析

```typescript
// 调用 Claude API
const response = await fetch('/api/rewrite', {
  method: 'POST',
  body: JSON.stringify({ mode, role, original }),
});

// 服务端(Next.js Route Handler)调用 Claude
import Anthropic from '@anthropic-ai/sdk';
const client = new Anthropic();
const msg = await client.messages.create({
  model: 'claude-sonnet-4-5',
  max_tokens: 4000,
  messages: [{ role: 'user', content: prompt }],
});

// 解析 JSON 容错处理
function parseAIResponse(raw: string) {
  let clean = raw.trim();
  // 剥掉可能的 markdown 包裹
  clean = clean.replace(/^```json\s*/i, '').replace(/^```\s*/, '').replace(/\s*```$/, '');
  // 找到第一个 { 和最后一个 }
  const first = clean.indexOf('{');
  const last  = clean.lastIndexOf('}');
  if (first >= 0 && last > first) clean = clean.slice(first, last + 1);
  return JSON.parse(clean);
}
```

**错误处理:**
- 解析失败时进入 error 阶段,展示错误信息 + 原始返回(折叠 details 标签)+ 重试按钮
- 建议加流式输出(Vercel AI SDK 的 streamText),但 MVP 可先用非流式

---

## 6. 实现细节与建议

### 6.1 项目初始化
```bash
npx create-next-app@latest resume-rewriter --typescript --tailwind --app
cd resume-rewriter
npm install @anthropic-ai/sdk ai @ai-sdk/anthropic
```

### 6.2 文件结构建议
```
app/
  page.tsx                    # 主组件,管理 stage 状态
  api/
    rewrite/route.ts          # Claude API 路由
  layout.tsx
components/
  stages/
    ModeStep.tsx
    RoleStep.tsx
    InputStep.tsx
    LoadingTerminal.tsx
    AutoResult.tsx
    ReviewResult.tsx
    ErrorPanel.tsx
  ui/
    Dot.tsx
    Tag.tsx
    TopBar.tsx
lib/
  prompts.ts                  # buildRewritePrompt / buildReviewPrompt
  parser.ts                   # parseAIResponse
  constants.ts                # ROLES, MODES, COLORS
styles/
  globals.css                 # CSS 变量、keyframes
```

### 6.3 环境变量
```env
ANTHROPIC_API_KEY=sk-ant-...
```

### 6.4 推荐增强(MVP 之后)
- **流式输出**:用 Vercel AI SDK 的 `streamText`,改写过程逐字出现,体验质感大幅提升
- **导出**:把改写结果导出 PDF / Markdown / Word
- **历史记录**:Supabase + Auth,保存每次改写
- **A/B 对比**:同一份简历跑三个岗位看哪个匹配度最高
- **JD 匹配**:让用户粘 JD,AI 按 JD 精准改写
- **付费**:免费 3 次,付费解锁无限 + 历史 + PDF 导出

### 6.5 注意事项
- **API Key 安全**:Claude API 调用必须在服务端,绝不能在前端暴露
- **rate limit**:加简单的客户端节流,避免重复点击
- **长输入**:textarea 字符上限建议 8000,超过提示截断或分段
- **响应解析**:Claude 偶尔会带 markdown 包裹,务必走 `parseAIResponse` 容错
- **Token 消耗**:一次完整改写约 2000-4000 tokens,成本约 $0.02-0.05(Sonnet 4),适合按次计费

---

## 7. 验收清单

实现完成后逐项检查:

- [ ] 4 个阶段(mode/role/input/loading/result)可以顺利切换,可返回上一步
- [ ] 顶部导航实时高亮当前 step
- [ ] 主色调严格按色板,**没有任何紫色出现**
- [ ] 等宽字体加载正常(JetBrains Mono),非系统默认 mono
- [ ] 点 EXECUTE 真的调 Claude API,不是假数据
- [ ] 快速重写模式 3 个 Tab 切换正常,双栏对比 / 改写全文 / 改动详情各自正确
- [ ] 精修模式 ACCEPT 按钮可切换状态,卡片 opacity 变化
- [ ] Loading 终端动画流畅,逐行出现 + 末尾光标闪烁
- [ ] Tag 组件、Dot 组件复用,样式一致
- [ ] AI 返回 JSON 解析容错(带 markdown 包裹也能解析)
- [ ] 错误兜底:API 失败有友好提示,可看原始返回 + 重试
- [ ] 响应式:至少 1280px+ 桌面端完美,移动端可降级
- [ ] 性能:首屏 LCP < 2s,无明显卡顿

---

## 8. 设计参考截图描述

如果做不出感觉,参考这些方向:
- **Vercel 官网**:黑底 + 等宽字体 + 极简卡片
- **Linear 设置页**:1px 分隔线、克制的色彩
- **Raycast 落地页**:终端美学但不死板
- **Cursor 官网**:IDE 隐喻、技术感
- **Anthropic 文档**:留白和层次感

**避免参考**:
- 任何带紫色渐变的 SaaS 落地页
- Bootstrap 风格的圆角卡片堆叠
- 看起来像 ChatGPT 包装的 wrapper 站